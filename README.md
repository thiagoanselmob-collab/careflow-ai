# CareFlow AI Backend

Backend service for CareFlow AI, built with FastAPI, SQLAlchemy, asyncpg, Redis, and Pytest.

## Setup

Ensure you have Python 3.11+ and Poetry installed.

```bash
poetry install
```

## Running the app

```bash
poetry run uvicorn app.main:app --reload
```
